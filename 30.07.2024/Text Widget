import tkinter as tk

def show_text():
    print(text.get("1.0", tk.END))

root = tk.Tk()
text = tk.Text(root)
text.pack()
button = tk.Button(root, text="Show Text", command=show_text)
button.pack()
root.mainloop()
