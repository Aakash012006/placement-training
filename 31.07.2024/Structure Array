#include <stdio.h>

struct Student {
    int id;
    char name[50];
};

int main() {
    struct Student students[3] = {{1, "John"}, {2, "Jane"}, {3, "Jim"}};
    for(int i = 0; i < 3; i++) {
        printf("ID: %d, Name: %s\n", students[i].id, students[i].name);
    }
    return 0;
}
