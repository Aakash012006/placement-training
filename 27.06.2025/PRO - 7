def linear_search(arr, target):
    for i, item in enumerate(arr):
        if item == target:
            return i
    return -1

nums = [4, 2, 9, 7, 5]
n = int(input("Enter number to find: "))
res = linear_search(nums, n)
print("Found at index", res) if res != -1 else print("Not found")
